# Tasks

## to do

- Optional: set real DEEPSEEK_API_KEY, GROK_API_KEY, OPENAI_API_KEY, FIRECRAWL_API_KEY, EXA_API_KEY, RESEND_API_KEY, or AGENTMAIL_API_KEY + AGENTMAIL_INBOX_ID on the deployment (CONTEXT_DEV_API_KEY is already real on prod)

## completed

- 2026-08-12 18:18 UTC: Added DeepSeek (default, deepseek-v4-flash) and Grok (grok-4.5) AI providers. Both use the OpenAI-compatible SDK against their own base URLs. Schema, prefs, capabilities, Settings picker, Ask labels, Docs, README, Compare, and the landing BYOK card now list all five providers. Leftover three-provider copy on landing, compare, settings integrations, and the docs env table is gone.

- 2026-08-11 19:57 UTC: Fixed the demo:reset crash "Too many functions scheduled by this mutation (limit: 1000)". PRD: prds/demo-reset-scheduled-function-limit.md. Root cause: dealsByOwner was namespaced by owner user id, the ten minute reseed mints new ids every run, the aggregate component never deletes a namespace, and clearAll schedules one job per namespace, so the count crossed 1000 and every reset rolled back. dealsByOwner had zero readers, so it was removed from convex/convex.config.ts, convex/aggregates.ts, and convex/demo.ts; dealsByStage stays (six fixed stage namespaces). Verified: convex dev --once, check-types, and lint clean; npx convex run demo:reset succeeds; dashboard:summary returns correct rollups. The unmounted dealsByOwner data can be deleted from the Convex dashboard on dev and prod.

- 2026-08-11 19:20 UTC: Adopted both community PRs and landed follow-ups. PRD: prds/adopt-community-prs.md. Cherry-picked PR 1 (AGENTS.md rewrite for the Convex port, every claim verified against the repo first) and PR 2 (restored link hover states in src/pages/Docs.tsx and src/app/Settings.tsx) keeping Fagner Sales as author. Follow-ups: fixed the stale docs/deploy.md reference in convex/model/access.ts, added a shared TextLink component to src/components/ui.tsx and migrated Docs, Settings, Landing, and ComposeEmail to it (Compare keeps its neutral treatment plus the 2px offset), and bumped @convex-dev/workflow 0.4.4 to 0.4.5 after a version pass over convex.config.ts components. Verified: npm run check-types and npm run lint clean.

- 2026-08-10 00:25 UTC: Updated the ForkIt section copy on the landing page (src/pages/Landing.tsx) from "One npm install, one Convex deployment" to "One clone, one Convex deployment" so the pitch is package manager neutral. The repo itself still documents npm install in the README and Docs, which stays accurate for the default path.

- 2026-08-09 23:49 UTC: Updated the three demo team user emails in convex/model/seed.ts from @trycomp.ai to @realtimecrm.dev (patrick, dan, lewis). Sam Whitfield's contact email and the Comp AI company domain stay on trycomp.ai since they are seeded company data, not team members. Convex dev pushed the change; the demo shows the new emails after the next 10 minute reset. Verified: no lint errors, functions pushed clean.

- 2026-08-09 23:42 UTC: Fixed the demo banner and warning callout font color in light mode. The three amber warning surfaces (DemoBanner, the Settings API key note, the Docs demo callout) all use text-amber-200/90, which was never remapped in the light theme, so pale amber text sat on a near-white background. Declared --color-amber-200 and --color-amber-500 in src/index.css following the existing token pattern: dark keeps the Tailwind defaults, light remaps text to a deep amber (#92400e, about 7:1 on white) and the border/tint to amber 600 (#d97706). Audited every raw palette class in src; amber was the only one missing a light remap (bg-emerald-600 toggle track reads fine on both themes). Verified: screenshots of the banner on localhost:5174 in light and dark mode.

- 2026-08-09 23:26 UTC: Added two missing rows to the Compare page (src/pages/Compare.tsx) after checking recent CHANGELOG entries: a Slack row (upstream not included; this port has optional event notifications plus the /crm bot over signed HTTP actions, off by default) and a Tables and custom fields row (per-entity column rename, hide, pin, new-record defaults, and four custom field types as real columns). Verified: eslint and tsc clean.

- 2026-08-09 23:20 UTC: Folded the standalone "Agents that automate your CRM" section into the WhatItDoes bento on the landing page (src/pages/Landing.tsx) and added three new feature mocks: MockAgentBuilder (agent prompt input plus seeded prompt cards next to a "Your agents" panel showing the deployed Renewal briefer and draft Stale deal flagger), MockSlack (a #deals channel message for a Roo Capital stage change plus a /crm slash command chip), MockWebResearch (search_web and read_page tool call rows with the Exa and Firecrawl labels and a recorded fact), and MockAsk (a /task slash command exchange with the reminder confirmation and command chips). Rebalanced the grid: agent builder and companies span two columns, record chat stays the tall right rail, Slack, web research, and Ask sit as a middle row of three, activity log spans two, BYOK spans the full row. Removed the AgentsSection component. Verified: eslint and tsc clean, screenshots on localhost:5174 in dark and light mode.

- 2026-08-09 23:08 UTC: Rebuilt the landing page WhatItDoes section (src/pages/Landing.tsx) as a three column bento with JSX mock UI blocks: seeded companies table with logos and enrichment badges, record chat with a Q and A exchange, suggested agents, follow-up rechecks with day chips, a mono activity log, and a new BYOK card grouping every optional provider (Chat, Research, Email) with its real env key name and a link to /docs#environment-variables. Moved DemoVideo below the section. PRD: prds/landing-what-it-does-bento.md. Verified: eslint and tsc clean, screenshots on localhost:5174 in dark and light mode.

- 2026-08-09 23:00 UTC: Added the Rybbit analytics script tag (site id b6159d6f09ad, deferred) to the head of index.html, after the theme script. One tag covers every page since the app is a SPA. Verified: tag present in the dev server response on localhost:5174.

- 2026-08-09 22:58 UTC: Rebuilt the landing page DemoNotes section (src/pages/Landing.tsx) as a bullet list with dot markers, soft accent highlights (Hl for phrases, EnvKey mono pills for env var names), and a docs link per line to the matching section id (#using-the-app, #fork-and-setup, #auth, #email, #slack, #web-research, #ai-providers). Verified: eslint and tsc clean, screenshots on localhost:5174 in dark and light mode.

- 2026-08-09 22:50 UTC: Modernized every checkbox in the app. Rebuilt the shared Checkbox (src/components/ui.tsx) with appearance-none, an inset well, accent-blue checked fill, and an animated white checkmark, plus hover, focus-visible, and disabled states. Replaced the last native checkbox in the Settings sidebar section (src/app/Settings.tsx) with the shared component, so all consumers (dataTable, EntitySettingsSection, Activity, Timeline, Settings) share one themed control. Verified in the browser on Settings sidebar and Companies column settings: checked, unchecked, and disabled states render correctly.

- 2026-08-09 22:45 UTC: Updated the footer credit line in SiteFooter (src/pages/Landing.tsx) to "Made with Cursor, Claude Fable and DNS Cloudflare." with each name linked (cursor.com, claude.com, developers.cloudflare.com/dns). Shows on the homepage and Compare page through the credit prop. Verified: eslint clean.

- 2026-08-09 22:35 UTC: Rewrote the docs custom domain section (src/pages/Docs.tsx) to the apex-first setup verified live on realtimecrm.dev after the DNS debugging session: apex CNAME to convex.domains proxied, www CNAME to the apex proxied, TXT verification, Full (strict) SSL, www-to-root redirect rule matching www.yourdomain.com exactly, CONVEX_SITE_URL override, and curl verification. Added a troubleshooting list mapping each real failure to its cause (redirect loop from the rule matching the apex, error 1014 from www pointing at convex.domains, 403 from the Convex entry drifting from DNS, cert minting delay, cached 301s) and a closing part on driving the setup with a scoped Cloudflare API token or the Cloudflare MCP servers. Added a new "Email DNS with Cloudflare" section covering Resend (Domain Connect auto setup or the manual MX plus two TXT records) and AgentMail (default agentmail.to inbox with zero DNS, custom domain records from the console), with the DNS only and single-SPF rules and the sending subdomain recommendation. Verified: eslint clean.

- 2026-08-09 20:05 UTC: Rewrote step 5 of the docs custom domain section (src/pages/Docs.tsx) to match Cloudflare's current root to www redirect guidance. The step now leads with the wildcard pattern method (Request URL https://yourdomain.com/*, Target URL https://www.yourdomain.com/${1}, 301, preserve query string), points at Rules, Overview, Create rule as the dashboard path, explains that ${1} carries the path, notes that other subdomains pass through untouched, and keeps the custom filter expression with concat() as an equivalent alternative. Verified: eslint clean.

- 2026-08-09 19:58 UTC: Per-entity settings, renameable columns, improved custom fields, and an upgraded table view. New tableSettings table and module store column preferences (rename, hide, pin) and new-record defaults per entity; Settings gained Companies, Contacts, and Deals sections (src/app/EntitySettingsSection.tsx) with defaults, column rename with restore, and a custom fields manager covering all four types plus archive and restore. Shared table infrastructure (src/components/dataTable.tsx, src/lib/columns.ts) drives Companies, Contacts, and the Deals list: per-column sort, pin with sticky offsets, hide, a Columns chooser, and click-to-edit custom field cells fed by the batch fields.tableValues query. Create mutations apply the saved defaults, the New deal form prefills stage and currency, and custom fields are click-to-edit on Company and Contact detail. Mobile: pinned columns stay sticky in the panel scroll and menus render in portals. Verified: eslint and both tsc typechecks clean, convex dev push green.

- 2026-08-09 17:25 UTC: Added a "Built with Cursor and Fable." credit line below the footer tagline. SiteFooter (src/pages/Landing.tsx) gained a credit prop so the line shows on the homepage and Compare page (src/pages/Compare.tsx) but not on Docs, which shares the same footer. Cursor links to cursor.com. Verified: eslint and tsc clean.

- 2026-08-09 17:10 UTC: Calendar due dates for tasks. PRD: prds/task-due-date-calendar.md. Added a themed DateInput calendar popover to src/components/ui.tsx, an "in days / on date" toggle in the task composer (src/components/Timeline.tsx) with calendar picks landing at 9:00 local, date labels ("due Aug 15") in the feed for tasks more than 7 days out via a new shortDate helper (src/lib/format.ts), and explicit date parsing in the Ask /task command (convex/ask.ts): "on Aug 15", "on 8/15", "on 2026-08-15", with year rollforward and a "Due Aug 15" confirmation. Synced the /task slash hint and the docs page copy. Verified: eslint and both tsc typechecks clean.

- 2026-08-09 16:45 UTC: Restyled the demo mode banner to match the amber Slack untested callout (border-amber-500/30, bg-amber-500/5, bold "Demo mode." lead-in) in src/components/DemoBanner.tsx. Added a "Demo users" amber callout to Settings, Team (src/app/Settings.tsx) gated on api.demo.info demoMode, so forks with demo mode off never see it. Verified: eslint and tsc clean.

- 2026-08-09 16:40 UTC: Enabled GitHub Issues on the fork (gh repo edit --enable-issues) and added "File an issue" links pointing at github.com/waynesutton/trycrm-convex/issues: one in the site footer (covers the homepage and Compare page, which share SiteFooter) and one pinned below the section list in the docs sidebar. Verified: eslint clean on both files.

- 2026-08-09 16:30 UTC: Docs prod pass. Added an amber "Untested" callout at the top of the Slack section (docs page and README) since the integration has not run against a live Slack workspace. Added --prod command coverage across the docs: the three Slack env commands each show a second --prod line, and the fork setup, email, web research, and AI provider sections each gained a --prod example plus a note that dev and production keep separate variables. README Slack intro mentions the twice-run pattern. Verified: eslint and tsc clean.

- 2026-08-09 15:05 UTC: Added a search box to the top of the docs sidebar (src/pages/Docs.tsx). Filters the section list by title and by the rendered body text of each section, shows "No sections match." when nothing hits, and Escape clears the query. Verified: eslint clean, tsc clean.

- 2026-08-09 12:10 UTC: Slack integration, off by default. PRD: prds/slack-integration.md. Phase 1: outbound notifications from company, contact, and deal creation, deal stage changes (through a new shared model/deals.ts helper), task completions, and agent run summaries, delivered through convex/slack.ts with the action retrier, message capping, deep links, demo mode guard, and Activity page logging; two connection modes (SLACK_WEBHOOK_URL or SLACK_BOT_TOKEN, bot wins when both are set); Settings gained a Slack page with a master switch, env badges, per-event toggles, a channel picker with search backed by conversations.list, a send test button, and the bot switch with an allowed email domain. Phase 2: the /crm bot in convex/slackBot.ts behind its own toggle, three signed HTTP routes under /webhooks/slack/ with v0 signature verification and three second acks, Slack user to member identity matching cached in the new slackIdentities table, and find, deal, note, task, activity, help commands reusing the model helpers with writes attributed as Name (Slack). Docs, README, landing built-with row, and env tables all updated with Slack setup links. Verified: eslint clean, both typechecks clean, dev push succeeded. Not runtime tested against a real Slack workspace (none available); implementation follows Slack's webhook, chat.postMessage, conversations.list, and request signing docs.

- 2026-08-09 11:50 UTC: Mobile pass across the app and marketing pages. PRD: prds/mobile-pass.md. App shell gains a mobile top bar and a right side nav drawer below md; tables on Companies, Contacts, Deals list, and Activity scroll inside their panels; filters and form fields go full width on phones; Ask stacks its thread sidebar with touch-visible row actions; compose email becomes a bottom sheet; Command-K and the shortcuts modal get mobile positioning; detail pages wrap headers, stack stats, and scroll tabs; landing and Compare fixed for narrow widths. Verified: eslint and tsc clean, and every route measured at exactly 375px scroll width in the browser with the drawer confirmed working.

- 2026-08-09 11:35 UTC: Made the demo reset cron safe for forks. PRD: prds/disable-demo-reset-for-forks.md. demo:reset now no-ops with an Activity log skip when demo mode is off; new demo:disableDemoMode internal mutation flips the flag with one CLI command; convex/crons.ts carries a FORKS comment; README gained a "Turning off the demo reset" section with a copy-paste coding agent prompt, and the /docs page carries the command in the auth section plus the prompt in the coding agents section. Verified: eslint clean on the touched files, and runtime tested on dev at 2026-08-09 11:55 UTC: demo:disableDemoMode flipped the flag, demo:reset logged "Demo mode is off, reset skipped" without wiping, then dev was restored to demo mode and a normal reset confirmed working. Repo-wide lint and tsc now pass with the mobile pass merged in; the safety stash from the session collision was verified fully applied and dropped.

- 2026-08-09 10:55 UTC: Reordered the deploy instructions in the docs deploy section and the README so the three --prod env sentinel commands come before npm run deploy; the old order pushed first, which fails on a fresh production deployment. Dropped the redundant npm run build. Redeployed static files so the live docs page shows the corrected steps. Committed and pushed as "docs: set prod env sentinels before first deploy"; the live site at good-dog-8.convex.site is current.

- 2026-08-09 10:50 UTC: Fixed the failed prod deploy. Root cause: dev and prod have separate env vars, and prod never got the EXA_API_KEY and FIRECRAWL_API_KEY sentinels. Tried making the app env vars optional first, but Convex rejects binding a required component var to an optional app var, so the "unset" sentinel stays the documented keyless path (comment updated in convex/convex.config.ts). Set both sentinels on prod and deployed backend plus static files to good-dog-8.convex.site. Verified: capabilities:status on prod shows contextDev true (real key), exa and firecrawl false, site returns 200. The live demo now has working web search and page reading through the Context.dev fallback.

- 2026-08-09 10:30 UTC: Context.dev is now a second provider for the chat agent's web research tools (convex/web.ts). Web search runs on Exa or Context.dev; page reading runs on Firecrawl or Context.dev; any one key enables its tool, the named provider leads when both keys are set, and a failed call falls back to the other configured provider. The same CONTEXT_DEV_API_KEY that powers brand enrichment covers both tools. Updated Settings integration rows, Compare rows, the docs web research section and env table, and the README. Verified: lint and both typechecks clean.

- 2026-08-09 10:40 UTC: Redid the landing demo video as a fully silent 20 second cut (public/demo.mp4, public/demo-poster.png). Two HeyGen Video Agent attempts collapsed silent videos to 7 and 8 seconds, so the final cut is composed locally with ffmpeg and ImageMagick from the same dark mode screenshots (heygen-assets/build-demo.sh): Helvetica type, tighter zooms on the pipeline stats, company rows, deal cards, agents, and Ask, the white Convex logo image everywhere Convex is referenced, and a "Just use" end card above the logo. No audio track at all. 2 MB h264 at 1080p; verified serving at /demo.mp4 on the dev server.

- 2026-08-09 10:00 UTC: Added a 20 second demo video to the landing page in a new "See it in 20 seconds" section above "What it actually does" (src/pages/Landing.tsx, public/demo.mp4, public/demo-poster.png). Generated with the HeyGen Video Agent from seven dark mode 1080p screenshots of the live app plus the white Convex logo. The API key stays in the HeyGen CLI user config; heygen-assets/ production scratch is gitignored. Verified in the browser: section placement, poster load, and playback.

- 2026-08-09 09:55 UTC: Added a terminal-style git clone one-liner with a copy button to the landing hero, sitting below the CTA buttons and above the Built with divider. Verified in the browser at localhost:5174; lint and typecheck clean.

- 2026-08-09 09:30 UTC: Added a Bcc field to the compose email window. The field hides behind a Bcc toggle next to Cc, flows through email.compose and sendComposed, and delivers natively on AgentMail; Resend sends each recipient an individual copy so Bcc addresses stay private. Timeline summaries include bcc, and the docs, README, and changelog mention the field. Verified: lint and both typechecks.

- 2026-08-09 09:45 UTC: Compose email, Settings sub-sidebar, and docs sidebar pass from prds/compose-email-settings-docs.md. New ComposeEmail window (draggable, resizable, To/Cc, markdown preview, Convex storage attachments) opened from Email buttons on company and contact detail; email.compose writes the EMAIL timeline activity and Activity log row, then sendComposed delivers through Resend or AgentMail with the signature appended and attachments as signed links, logging a skip when no key is set. Compose defaults (from name, from address, signature) live in Settings under Email, stored on the workspace. Settings restructured into /app/settings/:section pages with a sub-sidebar. Docs page rebuilt as a two-column layout with a sticky sidebar and active-section highlight; email section documents composing and links both component pages. Landing header GitHub link renamed to Fork. Verified: lint, both typechecks, dev push, and a 10-step browser pass (compose prefill, markdown preview, disabled Send explanation, settings round trip with save, docs sidebar scroll, Fork link).

- 2026-08-09 09:20 UTC: Keyboard shortcuts pass from prds/keyboard-shortcuts-modal.md. New ShortcutsModal listing every real shortcut, opened from a Phosphor keyboard icon next to the theme switcher or with Cmd ?; Cmd . toggles the sidebar. Landing h1 now reads "The CRM built for agents on Convex". Docs "Using the app" section mentions both new keys. Verified: lint, app typecheck, and a browser pass covering the headline, the icon and modal, Cmd ? open and Escape close, and Cmd . collapse and expand.

- 2026-08-09 09:10 UTC: Contrast and Settings key guidance pass from prds/contrast-settings-prod-docs.md. Muted grays raised to WCAG AA in both themes (src/index.css). Settings gained an "Adding API keys" panel showing npx convex env set for dev and with --prod for production, explaining the two-deployment split; every Integrations row links to its docs section, and the email and AI provider panels link to theirs. Docs page scrolls to hash anchors on client-side navigation. Verified: lint, app typecheck, and a browser pass confirming the panel, a Setup guide link scrolling to the email section, and readable helper text in light and dark mode.

- 2026-08-09 08:55 UTC: Notes, tasks, streaming, and polish pass from prds/notes-tasks-streaming-polish.md. Shared notes-and-tasks composer on company and contact detail (src/components/Timeline.tsx, convex/model/activities.ts) with due dates, email reminders through the selected provider, and complete buttons; timeline writes mirror to the Activity page. Ask gained /task and /note slash commands handled in the send mutation with record-name matching, "email me" reminders, and "in N days" due parsing. Ask replies now stream (streamText with saved deltas, useUIMessages stream: true) and the greeting covers late night. Full text search_name indexes on companies, contacts, deals power Command-K with a scan fallback. Themed Select, NumberInput, and Checkbox replaced every native control (Deals stage menus, filters, new deal form, recheck days). Sidebar collapses with the Phosphor SidebarSimple icon, persisted per browser. Activity page has select one / select all with logs.clearMany. Verified: lint, both typechecks, dev push with the new indexes, and a full browser pass covering themed dropdowns, sidebar collapse, activity multi-select, timeline note and task creation, /note in Ask with timeline confirmation, the late-night greeting with the honest missing-key reply, and Command-K results.
- 2026-08-09 08:25 UTC: Recaptured public/og.png (1200x630) from the updated landing page. The share image now says "The CRM built for agents on Convex" and shows both built-with rows including OpenAI, Claude, and OpenRouter. Verified dimensions with sips; index.html metadata already pointed at /og.png so no markup change was needed.
- 2026-08-09 08:20 UTC: Feature pass from prds/ask-tables-logs-polish.md verified: lint and both typechecks clean, convex dev push green, browser pass over Ask (missing-key reply), Command-K search results, Deals board/list toggle, Companies sort/filter/add row, Settings sidebar hide/show round trip, Activity log streaming with pause and clear.
- 2026-08-09 08:05 UTC: Feature pass from prds/ask-tables-logs-polish.md. Ask page (Claude-style workspace chat with slash commands, thread sub-sidebar, archive/delete) on convex/ask.ts. AI provider picker (OpenAI, Claude, OpenRouter) via convex/ai.ts and convex/prefs.ts; no AI key ships by default. Command-K search (convex/search.ts, CommandK.tsx). Activity log page with pause and clear (convex/logs.ts, Activity.tsx), log events from CRM mutations, agent tasks, email, chat, and the demo reset. Sidebar drag reorder plus show/hide in Settings. Deals board drag-and-drop plus sortable list view. Companies and Contacts sorting, filters, inline add rows. Landing spacing tightened, demo badge, fork link, built-with second row (OpenAI, Claude, OpenRouter). Docs rewritten: auth section around docs.convex.dev/auth/overview, AI providers section, static hosting notes. README, changelog, files.md synced.

- 2026-08-09 07:25 UTC: Added Firecrawl, Exa, and AgentMail components with keyless graceful degradation (convex/web.ts, convex/email.ts, convex/capabilities.ts, chat tools). Email provider toggle in Settings backed by workspace.emailProvider. New /docs page for non-devs linked in header, footer, and sidebar. Composio dark + Minimax light themes with a persisted toggle (src/index.css, ThemeToggle.tsx, inline script in index.html). OG/Twitter metadata with public/og.png (1200x630 dark screenshot). README rewritten with live links (convex.link/crmonconvex), new components, MIT license note. Compare page rows for the new components. Setup prompt now covers all three sentinel keys. PRD: prds/components-docs-theme.md. Verified: lint, both typechecks, dev push to outstanding-deer-221, browser checks of landing (both themes), settings toggle round trip, and the docs page.

- 2026-08-09 06:43 UTC: Synced project docs (task.md, files.md) ahead of the first push to github.com/waynesutton/trycrm-convex. Verified .gitignore keeps .env.local, node_modules, and dist out of the commit.
- 2026-08-09 06:37 UTC: Restyled the landing page and demo app to the GitHub features design system (Mona Sans, `#0d1117` canvas, cobalt primary buttons, tighter radii). Header now reads "CRM on Convex" with the logo moved to the Built with wall (white Convex, React, Vite, context.dev). Removed the demo banner reset button. Fixed the cloud deploy CONTEXT_DEV_API_KEY error by setting it on the deployment while keeping "unset" in the copy prompt. Lint, typecheck, and browser checks pass.
- 2026-08-09 06:05 UTC: Added the Convex ESLint plugin (flat config, type aware) and fixed all findings, including explicit table names on every db.get/patch/replace/delete. Added writeMutation from convex-helpers customFunctions so the write access check runs in one wrapper. Lint, both typechecks, dev push, and a live create plus cascade delete all pass.
- 2026-08-09 05:45 UTC: Full Convex port verified end to end. PRD: prds/convex-port.md. Typecheck and build pass; browser verification confirmed landing page, compare page, dashboard with live stats, demo banner countdown, companies table with seeded data, record chat with honest no-key reply, kanban stage moves, agent builder, and settings.
- 2026-08-09 05:40 UTC: Frontend built: landing, compare, dashboard, companies, contacts, deals board, agents, settings, demo banner. Files in src/.
- 2026-08-09 05:25 UTC: Backend complete: schema, CRM functions, agent queue with workpools, Context.dev enrichment with action cache and rate limiter, record chat on the agent component, Resend stub, crons (10 minute demo reset, 1 minute agent tick), static hosting routes. Deployed cleanly to an anonymous local deployment.
- 2026-08-09 04:50 UTC: Removed upstream monorepo (apps/, packages/, bun, Turborepo). Single npm package with convex/ and src/.
- 2026-08-09 04:30 UTC: Read all docs in docs/try-crm-instructions, wrote PRD at prds/convex-port.md.
- 2026-08-09 04:15 UTC: Merged upstream trycompai/crm into the fork locally, preserving docs/.

## notes

- CONTEXT_DEV_API_KEY, FIRECRAWL_API_KEY, and EXA_API_KEY are required by their components; the literal value "unset" is the documented sentinel for keyless installs. Remember to set all three with --prod before the first production deploy.
- The demo reset cron wipes all tables; remove it in convex/crons.ts for real use.
- Local verification used CONVEX_AGENT_MODE=anonymous so the user's Convex account was untouched.
